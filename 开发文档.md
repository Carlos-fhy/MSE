# 迷你低代码平台开发文档

## 项目概述

### 项目目标
构建一个轻量级低代码平台，支持通过配置 Schema 动态生成 CRUD 页面，并基于该平台实现一个简单的 MES（制造执行系统）示例。

### 技术栈

**前端**
- React 18 + TypeScript
- Vite 5.x（构建工具）
- Ant Design 5.x（UI 组件库）
- Monaco Editor（JSON 配置编辑器）
- Recharts / ECharts（图表库）
- React Gantt Chart（甘特图库，可选）
- React Router 6（路由管理）
- Axios（HTTP 客户端）
- XLSX（Excel 数据处理）
- date-fns（日期处理工具）

**后端**
- Node.js + TypeScript
- Express（Web 框架）
- Prisma（ORM）
- SQLite（数据库）
- JWT（jsonwebtoken，用户认证）
- Bcrypt（密码加密）
- Multer（文件上传）
- ExcelJS（Excel 生成）
- Socket.IO（可选，实时通信）

### 功能范围

本项目采用**分层架构**，按功能复杂度和开发优先级分为四个层级：

#### 第一层：低代码平台核心引擎（必须实现）

**配置管理**
- Schema 配置编辑器（Monaco Editor + JSON）
- Schema 保存、加载、删除
- Schema 格式验证

**动态渲染引擎**
- DynamicForm 组件（根据 Schema 渲染表单）
  - 支持字段类型：text、number、date、select、textarea
  - 自动表单验证（必填项、类型校验）
- DynamicTable 组件（根据 Schema 渲染表格）
  - 支持分页、排序、筛选
  - 支持自定义列宽、列显示

**通用 API**
- 统一的 RESTful CRUD API（`/api/data/:entity`）
- 支持任意实体的增删改查
- 支持分页、排序、筛选参数

---

#### 第二层：平台基础设施（必须实现）

**认证授权系统**
- JWT Token 认证
- 用户登录/登出
- 密码 Bcrypt 加密
- Token 自动刷新机制

**权限管理系统（RBAC）**
- 角色管理（CRUD）
- 用户管理（CRUD + 分配角色）
- 权限配置（resource:action 格式）
- 前后端双重权限校验
  - 路由级权限控制
  - API 接口权限中间件
  - 按钮级权限控制

**数据导入导出**
- Excel 导出（工单、报工、质检等）
- Excel 导入（含模板下载）
- 数据验证和错误提示

---

#### 第三层：MES 示例应用 - 低代码生成页面（必须实现）

**通过配置 Schema 生成以下 6 个 CRUD 页面：**

1. **工单管理**
   - 字段：工单号、产品名称、数量、状态、优先级、计划时间、绑定设备
   - 功能：创建、查看、编辑、删除、状态更新
   - 关联：报工记录、质检记录

2. **报工记录**
   - 字段：关联工单、报工员工、工时、完成数量、报工日期
   - 功能：创建、查看、编辑、删除
   - 统计：按工单/员工/日期统计工时

3. **质检记录**
   - 字段：关联工单、质检员、检验结果、检验数量、合格数量、不良数量
   - 功能：创建、查看、编辑、删除
   - 统计：质检合格率

4. **设备管理**
   - 字段：设备编号、设备名称、类别、型号、状态、位置
   - 功能：创建、查看、编辑、删除、状态更新

5. **用户管理**
   - 字段：用户名、真实姓名、角色、邮箱、电话、状态
   - 功能：创建、查看、编辑、删除、启用/禁用

6. **角色管理**
   - 字段：角色名称、描述、权限列表
   - 功能：创建、查看、编辑、删除、权限配置

**核心验证点：**
- ✅ 这 6 个页面完全通过 Schema 配置生成，无需编写 React 组件
- ✅ 证明低代码平台的核心能力
- ✅ 数据关联正确（工单 → 报工/质检）

---

#### 第四层：高级可视化页面（可选，时间允许再实现）

**这些功能需要额外开发 React 组件，不是低代码生成：**

1. **实时看板（DashboardPage）**
   - 4个核心指标卡片（今日工单、完成率、质检率、设备率）
   - 图表展示（饼图、折线图）
   - 设备状态矩阵
   - 自动刷新（轮询或 WebSocket）

2. **周视图（WeekViewPage）**
   - 甘特图视图（工单排程可视化）
   - 设备日历视图（7天网格）
   - 工单列表视图
   - 周导航和数据统计

3. **设备维护管理**
   - 维护记录列表
   - 维护类型（计划性/故障维修）
   - 维护历史追踪

**说明：**
- 这些是增强功能，让 MES 系统更完善
- 如果时间不够，可以不实现，不影响核心交付
- 实现了可以作为加分项

---

### 开发优先级

```
优先级 1（核心交付，13-19天）
  ├─ 低代码引擎（5-7天）
  ├─ 基础设施（4-6天）
  └─ MES 基础页面（4-6天）

优先级 2（增强功能，6-8天）
  ├─ 实时看板（3-4天）
  └─ 周视图（3-4天）
```

**交付策略：**
- 先完成优先级 1，确保核心功能完整
- 时间充裕再做优先级 2，锦上添花

---

## 系统架构

### 分层架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    前端应用 (React + TypeScript)                 │
├─────────────────────────────────────────────────────────────────┤
│  第四层：高级可视化页面（可选）                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ 实时看板     │  │ 周视图       │  │ 设备维护     │          │
│  │ Dashboard    │  │ WeekView     │  │ Maintenance  │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
├─────────────────────────────────────────────────────────────────┤
│  第三层：MES 示例应用（低代码生成）                               │
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐                   │
│  │工单│ │报工│ │质检│ │设备│ │用户│ │角色│ ← 通过Schema生成    │
│  └────┘ └────┘ └────┘ └────┘ └────┘ └────┘                   │
├─────────────────────────────────────────────────────────────────┤
│  第二层：平台基础设施                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ 认证授权     │  │ 权限管理     │  │ 导入导出     │          │
│  │ JWT + RBAC   │  │ Role/User    │  │ Excel I/O    │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
├─────────────────────────────────────────────────────────────────┤
│  第一层：低代码引擎                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ Schema Editor│  │ DynamicForm  │  │ DynamicTable │          │
│  │ 配置编辑器   │  │ 动态表单     │  │ 动态表格     │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                后端 API (Express + Prisma + TypeScript)          │
├─────────────────────────────────────────────────────────────────┤
│  认证中间件    │  权限中间件    │  Schema API   │  通用 CRUD API │
│  JWT verify   │  RBAC check   │  /api/schemas │  /api/data/:id │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                    SQLite 数据库 (Prisma ORM)                    │
├─────────────────────────────────────────────────────────────────┤
│  系统表         │  MES 业务表                                     │
│  - schemas      │  - work_orders     - work_reports              │
│  - users        │  - quality_checks  - equipments                │
│  - roles        │  - maintenance_records                         │
└─────────────────────────────────────────────────────────────────┘
```

### 核心设计理念

**1. 低代码引擎（第一层）**
- Schema 驱动：通过 JSON 配置描述页面结构
- 动态渲染：React 组件根据 Schema 自动生成 UI
- 类型安全：TypeScript 定义完整的 Schema 类型

**2. 平台基础设施（第二层）**
- 认证授权：JWT Token + Bcrypt 密码加密
- 权限管理：RBAC 模型，灵活的角色权限配置
- 导入导出：Excel 数据批量操作

**3. 示例应用（第三层）**
- 验证平台：通过 6 个 MES 页面证明低代码能力
- 数据关联：工单、报工、质检形成业务闭环
- 纯配置生成：无需编写页面组件代码

**4. 高级功能（第四层）**
- 增强体验：实时看板、周视图提供可视化能力
- 传统开发：使用 React 编写复杂交互组件
- 可选实现：时间充裕时锦上添花

### 核心设计：Schema 结构

```typescript
interface PageSchema {
  id: string;                    // 页面唯一标识
  name: string;                  // 页面名称
  entity: string;                // 对应的数据实体（如 work_orders）
  fields: FieldConfig[];         // 字段配置
  listConfig: ListConfig;        // 列表页配置
  formConfig: FormConfig;        // 表单页配置
}

interface FieldConfig {
  key: string;                   // 字段名
  label: string;                 // 显示标签
  type: 'text' | 'number' | 'date' | 'select' | 'textarea';
  required?: boolean;
  options?: { label: string; value: any }[];  // select 类型的选项
  defaultValue?: any;
}

interface ListConfig {
  columns: {
    key: string;
    title: string;
    width?: number;
    sorter?: boolean;
    filter?: boolean;
  }[];
  searchFields?: string[];       // 支持搜索的字段
  actions: ('create' | 'edit' | 'delete' | 'view')[];
}

interface FormConfig {
  layout: 'horizontal' | 'vertical';
  labelCol?: { span: number };
  wrapperCol?: { span: number };
}
```

---

## 数据库设计

### Prisma Schema

```prisma
// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

// ==================== 系统管理 ====================

// 用户表
model User {
  id        String   @id @default(uuid())
  username  String   @unique
  password  String   // bcrypt 加密
  realName  String
  email     String?
  phone     String?
  roleId    String
  role      Role     @relation(fields: [roleId], references: [id])
  status    String   @default("active") // active, disabled
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  workReports    WorkReport[]
  qualityChecks  QualityCheck[]
  createdOrders  WorkOrder[]
}

// 角色表
model Role {
  id          String   @id @default(uuid())
  name        String   @unique
  description String?
  permissions String   // JSON 数组，如 ["user:create", "order:view"]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  users User[]
}

// 页面配置表
model Schema {
  id        String   @id @default(uuid())
  name      String   @unique
  entity    String
  config    String   // JSON 字符串
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// ==================== MES 业务 ====================

// 工单表
model WorkOrder {
  id          String   @id @default(uuid())
  orderNo     String   @unique
  productName String
  quantity    Int
  status      String   // pending, in_progress, completed
  priority    String   @default("normal") // low, normal, high, urgent
  planStartDate DateTime?
  planEndDate   DateTime?
  actualStartDate DateTime?
  actualEndDate   DateTime?
  createdById String
  createdBy   User     @relation(fields: [createdById], references: [id])
  equipmentId String?
  equipment   Equipment? @relation(fields: [equipmentId], references: [id])
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  workReports    WorkReport[]
  qualityChecks  QualityCheck[]
}

// 报工表
model WorkReport {
  id          String   @id @default(uuid())
  workOrderId String
  workOrder   WorkOrder @relation(fields: [workOrderId], references: [id])
  userId      String
  user        User     @relation(fields: [userId], references: [id])
  workHours   Float
  completedQty Int     @default(0)
  reportDate  DateTime
  remark      String?
  createdAt   DateTime @default(now())
}

// 质检表
model QualityCheck {
  id          String   @id @default(uuid())
  workOrderId String
  workOrder   WorkOrder @relation(fields: [workOrderId], references: [id])
  inspectorId String
  inspector   User     @relation(fields: [inspectorId], references: [id])
  result      String   // pass, fail
  checkQty    Int      // 检验数量
  passQty     Int      // 合格数量
  defectQty   Int      @default(0) // 不良数量
  checkDate   DateTime
  remark      String?
  createdAt   DateTime @default(now())
}

// ==================== 设备管理 ====================

// 设备表
model Equipment {
  id            String   @id @default(uuid())
  equipmentNo   String   @unique
  equipmentName String
  category      String   // 设备类别：冲压、注塑、装配等
  model         String?  // 型号
  manufacturer  String?  // 制造商
  status        String   @default("idle") // idle, running, maintenance, fault
  location      String?  // 位置
  purchaseDate  DateTime?
  warrantyDate  DateTime?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  workOrders        WorkOrder[]
  maintenanceRecords MaintenanceRecord[]
  statusLogs        EquipmentStatusLog[]
}

// 设备维护记录表
model MaintenanceRecord {
  id          String   @id @default(uuid())
  equipmentId String
  equipment   Equipment @relation(fields: [equipmentId], references: [id])
  type        String   // planned(计划性维护), corrective(故障维修)
  description String
  startTime   DateTime
  endTime     DateTime?
  technician  String
  cost        Float?
  result      String?
  createdAt   DateTime @default(now())
}

// 设备状态日志表（用于实时看板）
model EquipmentStatusLog {
  id          String   @id @default(uuid())
  equipmentId String
  equipment   Equipment @relation(fields: [equipmentId], references: [id])
  status      String   // idle, running, maintenance, fault
  timestamp   DateTime @default(now())
  remark      String?
}
```

---

## 项目结构

```
project-root/
├── frontend/                    # 前端项目
│   ├── src/
│   │   ├── components/          # 通用组件
│   │   │   ├── SchemaEditor/    # Schema 编辑器
│   │   │   ├── DynamicForm/     # 动态表单
│   │   │   ├── DynamicTable/    # 动态表格
│   │   │   ├── Dashboard/       # 实时看板组件
│   │   │   │   ├── WorkOrderChart.tsx    # 工单统计图表
│   │   │   │   ├── EquipmentStatus.tsx   # 设备状态卡片
│   │   │   │   └── QualityTrend.tsx      # 质量趋势图
│   │   │   ├── WeekView/        # 周视图组件
│   │   │   │   ├── GanttView.tsx         # 甘特图视图
│   │   │   │   ├── CalendarView.tsx      # 日历视图
│   │   │   │   ├── ListView.tsx          # 列表视图
│   │   │   │   ├── WeekSelector.tsx      # 周选择器
│   │   │   │   └── WeekSummary.tsx       # 周统计摘要
│   │   │   └── ExportButton/    # 导出按钮组件
│   │   ├── pages/
│   │   │   ├── LoginPage/       # 登录页面
│   │   │   ├── ConfigPage/      # 配置管理页面
│   │   │   ├── RuntimePage/     # 运行时页面
│   │   │   ├── DashboardPage/   # 实时看板页面
│   │   │   ├── WeekViewPage/    # 周视图页面
│   │   │   ├── UserManagement/  # 用户管理页面
│   │   │   ├── RoleManagement/  # 角色管理页面
│   │   │   └── EquipmentManagement/ # 设备管理页面
│   │   ├── services/            # API 调用
│   │   │   ├── auth.service.ts
│   │   │   ├── user.service.ts
│   │   │   ├── equipment.service.ts
│   │   │   ├── dashboard.service.ts
│   │   │   ├── weekview.service.ts
│   │   │   └── export.service.ts
│   │   ├── hooks/               # 自定义 Hooks
│   │   │   ├── useAuth.ts
│   │   │   └── usePermission.ts
│   │   ├── utils/               # 工具函数
│   │   │   ├── auth.ts
│   │   │   └── export.ts
│   │   ├── types/               # TypeScript 类型定义
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── package.json
│   └── vite.config.ts
│
├── backend/                     # 后端项目
│   ├── src/
│   │   ├── routes/
│   │   │   ├── auth.routes.ts
│   │   │   ├── user.routes.ts
│   │   │   ├── role.routes.ts
│   │   │   ├── equipment.routes.ts
│   │   │   ├── dashboard.routes.ts
│   │   │   ├── weekview.routes.ts
│   │   │   ├── export.routes.ts
│   │   │   ├── schema.routes.ts
│   │   │   └── data.routes.ts
│   │   ├── controllers/
│   │   │   ├── auth.controller.ts
│   │   │   ├── user.controller.ts
│   │   │   ├── equipment.controller.ts
│   │   │   ├── dashboard.controller.ts
│   │   │   └── weekview.controller.ts
│   │   ├── services/
│   │   │   ├── auth.service.ts
│   │   │   ├── export.service.ts
│   │   │   └── websocket.service.ts
│   │   ├── middleware/
│   │   │   ├── auth.middleware.ts
│   │   │   └── permission.middleware.ts
│   │   ├── utils/
│   │   │   ├── jwt.ts
│   │   │   └── password.ts
│   │   └── index.ts             # 入口文件
│   ├── prisma/
│   │   └── schema.prisma
│   ├── package.json
│   └── tsconfig.json
│
└── docs/                        # 文档目录
    ├── 开发文档.md
    └── AI使用报告.md
```

---

## 新增功能详细设计

### 功能一：用户登录与权限管理

#### 1.1 用户认证

**登录流程**
```
用户输入用户名密码 → 后端验证 → 生成 JWT Token → 返回用户信息 + Token
→ 前端存储 Token（localStorage） → 后续请求携带 Token
```

**JWT Token 结构**
```typescript
interface JWTPayload {
  userId: string;
  username: string;
  roleId: string;
  roleName: string;
  permissions: string[];
  iat: number;  // 签发时间
  exp: number;  // 过期时间（24小时）
}
```

**认证中间件**
```typescript
// auth.middleware.ts
export const authMiddleware = async (req, res, next) => {
  const token = req.headers.authorization?.replace('Bearer ', '');
  if (!token) {
    return res.status(401).json({ error: 'Unauthorized' });
  }

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.user = decoded;
    next();
  } catch (error) {
    return res.status(401).json({ error: 'Invalid token' });
  }
};
```

#### 1.2 权限系统设计

**RBAC（基于角色的访问控制）**

**预置角色**
1. **超级管理员（admin）**
   - 所有权限
   - 可管理用户、角色、系统配置

2. **生产经理（production_manager）**
   - 工单：查看、创建、编辑、删除
   - 设备：查看、编辑
   - 报工：查看
   - 质检：查看
   - 看板：查看
   - 周视图：查看、编辑排程

3. **车间主任（workshop_director）**
   - 工单：查看、编辑
   - 设备：查看、编辑
   - 报工：查看、创建
   - 质检：查看
   - 周视图：查看、编辑排程

4. **普通员工（worker）**
   - 工单：查看（仅自己相关）
   - 报工：创建（仅自己）
   - 设备：查看
   - 周视图：查看

5. **质检员（quality_inspector）**
   - 工单：查看
   - 质检：查看、创建、编辑
   - 周视图：查看

**权限格式**
```typescript
// 格式：resource:action
const permissions = [
  'user:view', 'user:create', 'user:edit', 'user:delete',
  'role:view', 'role:create', 'role:edit', 'role:delete',
  'workorder:view', 'workorder:create', 'workorder:edit', 'workorder:delete',
  'workreport:view', 'workreport:create', 'workreport:edit', 'workreport:delete',
  'qualitycheck:view', 'qualitycheck:create', 'qualitycheck:edit', 'qualitycheck:delete',
  'equipment:view', 'equipment:create', 'equipment:edit', 'equipment:delete',
  'dashboard:view',
  'weekview:view', 'weekview:edit',
  'config:view', 'config:edit'
];
```

**权限检查中间件**
```typescript
// permission.middleware.ts
export const checkPermission = (requiredPermission: string) => {
  return (req, res, next) => {
    const userPermissions = req.user.permissions;

    if (!userPermissions.includes(requiredPermission)) {
      return res.status(403).json({ error: 'Forbidden' });
    }

    next();
  };
};

// 使用示例
router.post('/api/workorders',
  authMiddleware,
  checkPermission('workorder:create'),
  createWorkOrder
);
```

#### 1.3 前端权限控制

**路由守卫**
```typescript
// App.tsx
const ProtectedRoute = ({ children, requiredPermission }) => {
  const { user, hasPermission } = useAuth();

  if (!user) {
    return <Navigate to="/login" />;
  }

  if (requiredPermission && !hasPermission(requiredPermission)) {
    return <div>无权限访问</div>;
  }

  return children;
};
```

**按钮级权限控制**
```typescript
// 使用示例
const { hasPermission } = usePermission();

{hasPermission('workorder:create') && (
  <Button onClick={handleCreate}>创建工单</Button>
)}
```

---

### 功能二：设备管理

#### 2.1 设备基础管理

**功能清单**
- ✅ 设备档案管理（CRUD）
  - 设备编号、名称、类别、型号
  - 制造商、位置、采购日期、保修期

- ✅ 设备状态管理
  - 空闲（idle）
  - 运行中（running）
  - 维护中（maintenance）
  - 故障（fault）

- ✅ 设备与工单关联
  - 创建工单时选择使用的设备
  - 查看设备当前绑定的工单

#### 2.2 设备维护管理

**维护类型**
1. **计划性维护（Planned Maintenance）**
   - 定期保养
   - 提前安排时间
   - 记录维护项目

2. **故障维修（Corrective Maintenance）**
   - 设备故障后维修
   - 记录故障原因
   - 维修结果和费用

**维护记录字段**
```typescript
interface MaintenanceRecord {
  equipmentId: string;
  type: 'planned' | 'corrective';
  description: string;      // 维护内容
  startTime: Date;
  endTime: Date;
  technician: string;       // 技术人员
  cost: number;             // 费用
  result: string;           // 维护结果
}
```

#### 2.3 设备状态日志

**用途**
- 记录设备状态变更历史
- 为实时看板提供数据
- 统计设备利用率

**状态流转**
```
空闲 → 运行中 → 空闲
     → 维护中 → 空闲
     → 故障 → 维护中 → 空闲
```

---

### 功能三：实时看板（Dashboard）

#### 3.1 看板布局

**四大区域**
```
┌─────────────────────────────────────────┐
│  今日生产概况（数字卡片）                │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐  │
│  │工单数│ │完成率│ │质检率│ │设备率│  │
│  └──────┘ └──────┘ └──────┘ └──────┘  │
├─────────────────────────────────────────┤
│  工单状态分布（饼图）│ 质量趋势（折线图） │
├─────────────────────────────────────────┤
│  设备实时状态（状态卡片矩阵）             │
│  ┌────────┐ ┌────────┐ ┌────────┐      │
│  │设备A   │ │设备B   │ │设备C   │      │
│  │运行中  │ │空闲    │ │维护中  │      │
│  └────────┘ └────────┘ └────────┘      │
├─────────────────────────────────────────┤
│  最近报工记录（表格）                    │
└─────────────────────────────────────────┘
```

#### 3.2 数据统计指标

**核心指标（实时计算）**

1. **今日工单数**
   ```sql
   SELECT COUNT(*) FROM WorkOrder
   WHERE DATE(createdAt) = CURRENT_DATE
   ```

2. **工单完成率**
   ```sql
   SELECT
     COUNT(CASE WHEN status = 'completed' THEN 1 END) * 100.0 / COUNT(*) as rate
   FROM WorkOrder
   WHERE DATE(createdAt) = CURRENT_DATE
   ```

3. **质检合格率**
   ```sql
   SELECT
     SUM(passQty) * 100.0 / SUM(checkQty) as rate
   FROM QualityCheck
   WHERE DATE(checkDate) = CURRENT_DATE
   ```

4. **设备运行率**
   ```sql
   SELECT
     COUNT(CASE WHEN status = 'running' THEN 1 END) * 100.0 / COUNT(*) as rate
   FROM Equipment
   ```

**图表数据**

1. **工单状态分布（饼图）**
   ```typescript
   {
     待生产: 15,
     生产中: 8,
     已完成: 27
   }
   ```

2. **质量趋势（最近7天，折线图）**
   ```typescript
   [
     { date: '01-13', passRate: 98.5 },
     { date: '01-14', passRate: 97.2 },
     // ...
   ]
   ```

3. **设备状态分布**
   ```typescript
   {
     idle: 3,
     running: 5,
     maintenance: 1,
     fault: 1
   }
   ```

#### 3.3 实时更新机制

**方案一：轮询（Polling）**
```typescript
// 前端每 5 秒请求一次最新数据
useEffect(() => {
  const interval = setInterval(() => {
    fetchDashboardData();
  }, 5000);

  return () => clearInterval(interval);
}, []);
```

**方案二：WebSocket（推荐）**
```typescript
// 后端推送数据变更
io.on('connection', (socket) => {
  socket.on('subscribe:dashboard', () => {
    socket.join('dashboard');
  });
});

// 当工单、报工、质检数据变更时
io.to('dashboard').emit('dashboard:update', newData);
```

#### 3.4 技术选型

**图表库：ECharts 或 Recharts**
- ECharts：功能强大，样式丰富
- Recharts：React 组件化，易于集成

**实时通信：Socket.IO**
```bash
npm install socket.io socket.io-client
```

---

### 功能四：数据导入导出

#### 4.1 数据导出

**支持格式**
- ✅ Excel（.xlsx）
- ✅ CSV
- ✅ JSON

**导出功能点**
1. 导出当前页面数据
2. 导出全部数据
3. 导出筛选后的数据
4. 自定义导出字段

**技术实现（Excel）**

**前端**
```typescript
// 使用 xlsx 库
import * as XLSX from 'xlsx';

const exportToExcel = (data: any[], filename: string) => {
  const worksheet = XLSX.utils.json_to_sheet(data);
  const workbook = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(workbook, worksheet, 'Sheet1');
  XLSX.writeFile(workbook, `${filename}.xlsx`);
};
```

**后端**
```typescript
// 使用 exceljs 库
import ExcelJS from 'exceljs';

export const exportWorkOrders = async (req, res) => {
  const orders = await prisma.workOrder.findMany();

  const workbook = new ExcelJS.Workbook();
  const worksheet = workbook.addWorksheet('工单列表');

  worksheet.columns = [
    { header: '工单号', key: 'orderNo', width: 20 },
    { header: '产品名称', key: 'productName', width: 30 },
    { header: '数量', key: 'quantity', width: 10 },
    { header: '状态', key: 'status', width: 15 },
  ];

  worksheet.addRows(orders);

  res.setHeader(
    'Content-Type',
    'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'
  );
  res.setHeader(
    'Content-Disposition',
    'attachment; filename=workorders.xlsx'
  );

  await workbook.xlsx.write(res);
  res.end();
};
```

#### 4.2 数据导入

**导入流程**
```
1. 用户上传 Excel 文件
2. 后端解析文件
3. 数据验证（格式、必填项、类型）
4. 批量插入数据库
5. 返回导入结果（成功数、失败数、错误详情）
```

**前端组件**
```typescript
<Upload
  accept=".xlsx,.xls"
  beforeUpload={handleUpload}
  showUploadList={false}
>
  <Button icon={<UploadOutlined />}>导入 Excel</Button>
</Upload>
```

**后端实现**
```typescript
import multer from 'multer';
import * as XLSX from 'xlsx';

const upload = multer({ dest: 'uploads/' });

router.post('/api/import/workorders', upload.single('file'), async (req, res) => {
  const file = req.file;
  const workbook = XLSX.readFile(file.path);
  const sheetName = workbook.SheetNames[0];
  const worksheet = workbook.Sheets[sheetName];
  const data = XLSX.utils.sheet_to_json(worksheet);

  const results = {
    success: 0,
    failed: 0,
    errors: []
  };

  for (const row of data) {
    try {
      // 数据验证
      if (!row.orderNo || !row.productName || !row.quantity) {
        throw new Error('缺少必填字段');
      }

      // 插入数据库
      await prisma.workOrder.create({
        data: {
          orderNo: row.orderNo,
          productName: row.productName,
          quantity: parseInt(row.quantity),
          status: row.status || 'pending',
          createdById: req.user.userId
        }
      });

      results.success++;
    } catch (error) {
      results.failed++;
      results.errors.push({
        row: row,
        error: error.message
      });
    }
  }

  res.json(results);
});
```

#### 4.3 导入模板下载

**提供 Excel 模板**
- 包含示例数据
- 标注必填字段
- 说明数据格式

```typescript
router.get('/api/template/workorders', async (req, res) => {
  const workbook = new ExcelJS.Workbook();
  const worksheet = workbook.addWorksheet('工单导入模板');

  worksheet.columns = [
    { header: '工单号*', key: 'orderNo', width: 20 },
    { header: '产品名称*', key: 'productName', width: 30 },
    { header: '数量*', key: 'quantity', width: 10 },
    { header: '状态', key: 'status', width: 15 },
  ];

  // 添加示例数据
  worksheet.addRow({
    orderNo: 'WO-20250119-001',
    productName: 'iPhone 外壳',
    quantity: 1000,
    status: 'pending'
  });

  // 添加说明
  worksheet.addRow([]);
  worksheet.addRow(['说明：带 * 的字段为必填项']);
  worksheet.addRow(['状态可选值：pending(待生产), in_progress(生产中), completed(已完成)']);

  res.setHeader(
    'Content-Type',
    'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'
  );
  res.setHeader(
    'Content-Disposition',
    'attachment; filename=workorder_template.xlsx'
  );

  await workbook.xlsx.write(res);
  res.end();
});
```

---

### 功能五：周视图（Week View）

周视图是 MES 系统中用于生产计划排程和资源管理的重要工具，提供一周时间范围内的工单排程、设备占用情况的可视化展示。

#### 5.1 周视图布局设计

**整体布局**
```
┌────────────────────────────────────────────────────────────┐
│  周选择器：← 2025年第3周 (1月13日 - 1月19日) →           │
│  视图切换：[甘特图] [设备日历] [工单列表]                   │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  甘特图视图（工单排程）                                      │
│                                                             │
│  设备A  ████████████░░░░░░░░░░░░                           │
│         工单WO-001   工单WO-002                             │
│                                                             │
│  设备B  ░░░░████████████████░░░░                           │
│              工单WO-003                                     │
│                                                             │
│  设备C  ████░░░░░░░░████████████                           │
│         工单WO-004      工单WO-005                          │
│                                                             │
│         周一 周二 周三 周四 周五 周六 周日                   │
│                                                             │
├────────────────────────────────────────────────────────────┤
│  周统计摘要                                                  │
│  计划工单：15   完成工单：8   进行中：5   待生产：2          │
│  设备利用率：75%   平均完成率：53%                           │
└────────────────────────────────────────────────────────────┘
```

#### 5.2 三种视图模式

**模式一：甘特图视图（推荐）**

用横向条形图展示工单在时间轴上的分布，直观显示工单的计划时间、实际进度和设备占用情况。

**特性**
- 时间轴：横轴为一周的日期（周一到周日）
- 设备分组：纵轴按设备分组
- 工单块：每个工单显示为一个彩色矩形块
- 进度显示：通过颜色深浅或填充百分比显示完成进度
- 拖拽排程：支持拖拽工单块调整时间（可选）

**工单块信息**
```
┌─────────────────────┐
│ WO-001 (iPhone外壳) │
│ 计划: 1/13 - 1/15   │
│ 进度: 60%          │
│ 状态: 生产中        │
└─────────────────────┘
```

**颜色编码**
- 🟢 绿色：已完成
- 🔵 蓝色：进行中
- 🟡 黄色：待生产
- 🔴 红色：延期/告警

---

**模式二：设备日历视图**

以日历形式展示每台设备在一周内的占用情况。

**布局**
```
设备A
┌────┬────┬────┬────┬────┬────┬────┐
│ 周一│ 周二│ 周三│ 周四│ 周五│ 周六│ 周日│
├────┼────┼────┼────┼────┼────┼────┤
│WO-1│WO-1│WO-2│WO-2│空闲│空闲│空闲│
│100%│ 60%│ 80%│空闲│    │    │    │
└────┴────┴────┴────┴────┴────┴────┘

设备B
┌────┬────┬────┬────┬────┬────┬────┐
│维护│维护│WO-3│WO-3│WO-3│空闲│空闲│
└────┴────┴────┴────┴────┴────┴────┘
```

**特性**
- 每个格子代表一天
- 显示工单号和负载百分比
- 特殊状态标注（维护、故障）
- 点击格子查看详细信息

---

**模式三：工单列表视图**

以表格形式展示本周所有工单，支持按时间、设备、状态分组。

**列表字段**
| 工单号 | 产品名称 | 设备 | 计划开始 | 计划结束 | 状态 | 进度 | 操作 |
|--------|---------|------|---------|---------|------|------|------|
| WO-001 | iPhone外壳 | 设备A | 1/13 | 1/15 | 生产中 | 60% | 详情 |
| WO-002 | iPad外壳 | 设备A | 1/16 | 1/17 | 待生产 | 0% | 详情 |
| WO-003 | 充电器 | 设备B | 1/14 | 1/16 | 生产中 | 45% | 详情 |

**筛选功能**
- 按设备筛选
- 按状态筛选
- 按优先级排序
- 搜索工单号/产品名称

#### 5.3 数据计算逻辑

**工单进度计算**
```typescript
interface WorkOrderProgress {
  orderId: string;
  orderNo: string;
  productName: string;
  equipmentId: string;
  planStartDate: Date;
  planEndDate: Date;
  actualStartDate?: Date;
  status: string;
  progress: number;  // 0-100
  isDelayed: boolean;
}

// 进度计算公式（基于报工）
const calculateProgress = async (orderId: string) => {
  const order = await prisma.workOrder.findUnique({
    where: { id: orderId },
    include: { workReports: true }
  });

  const completedQty = order.workReports.reduce(
    (sum, report) => sum + report.completedQty,
    0
  );

  const progress = Math.min(
    (completedQty / order.quantity) * 100,
    100
  );

  return Math.round(progress);
};

// 延期判断
const isDelayed = (order: WorkOrder) => {
  if (order.status === 'completed') return false;
  const now = new Date();
  return now > order.planEndDate;
};
```

**设备负载计算**
```typescript
// 计算设备在某一天的负载百分比
const calculateEquipmentLoad = async (
  equipmentId: string,
  date: Date
) => {
  const orders = await prisma.workOrder.findMany({
    where: {
      equipmentId,
      AND: [
        { planStartDate: { lte: date } },
        { planEndDate: { gte: date } }
      ]
    }
  });

  // 假设每天工作 8 小时
  const totalHours = 8;
  let usedHours = 0;

  for (const order of orders) {
    // 简化计算：平均分配到每天
    const days = differenceInDays(order.planEndDate, order.planStartDate) + 1;
    usedHours += totalHours / days;
  }

  return Math.min((usedHours / totalHours) * 100, 100);
};
```

**周统计数据**
```typescript
interface WeekSummary {
  weekNumber: number;        // 第几周
  startDate: Date;           // 周开始日期
  endDate: Date;             // 周结束日期
  totalOrders: number;       // 计划工单总数
  completedOrders: number;   // 完成工单数
  inProgressOrders: number;  // 进行中工单数
  pendingOrders: number;     // 待生产工单数
  avgCompletion: number;     // 平均完成率
  equipmentUtilization: number;  // 设备利用率
  delayedOrders: number;     // 延期工单数
}
```

#### 5.4 技术实现

**前端：使用 Gantt 图表库**

**方案一：react-gantt-chart（推荐）**
```bash
npm install react-gantt-chart
```

```typescript
import { Gantt, Task } from 'react-gantt-chart';

const WeekView = () => {
  const [tasks, setTasks] = useState<Task[]>([]);

  useEffect(() => {
    // 将工单数据转换为甘特图任务
    const ganttTasks = orders.map(order => ({
      id: order.id,
      name: `${order.orderNo} - ${order.productName}`,
      start: new Date(order.planStartDate),
      end: new Date(order.planEndDate),
      progress: order.progress,
      type: 'task',
      project: order.equipment.equipmentName,
      styles: {
        backgroundColor: getStatusColor(order.status),
        progressColor: '#4CAF50'
      }
    }));

    setTasks(ganttTasks);
  }, [orders]);

  return (
    <div>
      <Gantt
        tasks={tasks}
        viewMode="Week"
        locale="zh-CN"
        onClick={handleTaskClick}
      />
    </div>
  );
};

const getStatusColor = (status: string) => {
  switch (status) {
    case 'completed': return '#4CAF50';
    case 'in_progress': return '#2196F3';
    case 'pending': return '#FFC107';
    default: return '#9E9E9E';
  }
};
```

**方案二：echarts Gantt（更灵活）**
```typescript
import ReactECharts from 'echarts-for-react';

const GanttChart = ({ data }) => {
  const option = {
    tooltip: {
      formatter: function (params) {
        return `
          工单：${params.name}<br/>
          开始：${formatDate(params.value[1])}<br/>
          结束：${formatDate(params.value[2])}<br/>
          进度：${params.value[3]}%
        `;
      }
    },
    xAxis: {
      type: 'time',
      min: startOfWeek(new Date()),
      max: endOfWeek(new Date())
    },
    yAxis: {
      type: 'category',
      data: equipmentNames
    },
    series: [{
      type: 'custom',
      renderItem: renderGanttItem,
      data: ganttData
    }]
  };

  return <ReactECharts option={option} />;
};
```

**方案三：自定义实现（最灵活）**
```typescript
// 使用 CSS Grid 布局实现简单甘特图
const SimpleGantt = ({ orders, equipments, weekDates }) => {
  return (
    <div className="gantt-container">
      {/* 时间轴头部 */}
      <div className="gantt-header">
        <div className="equipment-label">设备</div>
        {weekDates.map(date => (
          <div key={date} className="date-cell">
            {format(date, 'MM/dd')}
          </div>
        ))}
      </div>

      {/* 设备行 */}
      {equipments.map(equipment => (
        <div key={equipment.id} className="gantt-row">
          <div className="equipment-label">{equipment.name}</div>
          <div className="timeline">
            {orders
              .filter(o => o.equipmentId === equipment.id)
              .map(order => (
                <div
                  key={order.id}
                  className="order-block"
                  style={{
                    left: calculatePosition(order.planStartDate),
                    width: calculateWidth(order.planStartDate, order.planEndDate),
                    backgroundColor: getStatusColor(order.status)
                  }}
                >
                  {order.orderNo}
                </div>
              ))
            }
          </div>
        </div>
      ))}
    </div>
  );
};
```

**后端：周数据 API**

```typescript
// 获取指定周的工单数据
router.get('/api/week-view/:year/:week', authMiddleware, async (req, res) => {
  const { year, week } = req.params;

  // 计算周的开始和结束日期
  const weekStart = startOfWeek(new Date(parseInt(year), 0, 1 + (parseInt(week) - 1) * 7));
  const weekEnd = endOfWeek(weekStart);

  // 查询工单
  const orders = await prisma.workOrder.findMany({
    where: {
      OR: [
        // 工单在本周内开始
        {
          planStartDate: {
            gte: weekStart,
            lte: weekEnd
          }
        },
        // 工单跨越本周
        {
          AND: [
            { planStartDate: { lte: weekEnd } },
            { planEndDate: { gte: weekStart } }
          ]
        }
      ]
    },
    include: {
      equipment: true,
      workReports: true,
      qualityChecks: true
    },
    orderBy: {
      planStartDate: 'asc'
    }
  });

  // 计算每个工单的进度
  const ordersWithProgress = await Promise.all(
    orders.map(async (order) => ({
      ...order,
      progress: await calculateProgress(order.id),
      isDelayed: isDelayed(order)
    }))
  );

  // 计算周统计数据
  const summary = {
    weekNumber: parseInt(week),
    startDate: weekStart,
    endDate: weekEnd,
    totalOrders: orders.length,
    completedOrders: orders.filter(o => o.status === 'completed').length,
    inProgressOrders: orders.filter(o => o.status === 'in_progress').length,
    pendingOrders: orders.filter(o => o.status === 'pending').length,
    avgCompletion: ordersWithProgress.reduce((sum, o) => sum + o.progress, 0) / orders.length || 0,
    delayedOrders: ordersWithProgress.filter(o => o.isDelayed).length
  };

  // 计算设备利用率
  const equipments = await prisma.equipment.findMany();
  const equipmentLoads = await Promise.all(
    equipments.map(async (eq) => {
      const dailyLoads = [];
      for (let d = new Date(weekStart); d <= weekEnd; d.setDate(d.getDate() + 1)) {
        dailyLoads.push(await calculateEquipmentLoad(eq.id, new Date(d)));
      }
      return dailyLoads.reduce((sum, load) => sum + load, 0) / dailyLoads.length;
    })
  );
  summary.equipmentUtilization = equipmentLoads.reduce((sum, load) => sum + load, 0) / equipmentLoads.length || 0;

  res.json({
    summary,
    orders: ordersWithProgress,
    equipments
  });
});
```

#### 5.5 交互功能

**基础交互**
- ✅ 周切换（上一周/下一周）
- ✅ 跳转到指定周（日期选择器）
- ✅ 视图模式切换（甘特图/日历/列表）
- ✅ 点击工单块查看详情
- ✅ 悬停显示 Tooltip
- ✅ 缩放时间轴（可选）

**高级交互（可选）**
- 🔄 拖拽工单块调整时间
- 🔄 拖拽调整工单时长
- 🔄 拖拽工单到其他设备
- 🔄 右键菜单（编辑、删除）
- 🔄 批量操作

**数据刷新**
- 手动刷新按钮
- 定时自动刷新（60秒）
- WebSocket 实时更新（可选）

#### 5.6 权限控制

| 角色 | 查看 | 编辑排程 | 创建工单 | 删除工单 |
|------|------|---------|---------|---------|
| 超级管理员 | ✅ | ✅ | ✅ | ✅ |
| 生产经理 | ✅ | ✅ | ✅ | ✅ |
| 车间主任 | ✅ | ✅ | ❌ | ❌ |
| 普通员工 | ✅ | ❌ | ❌ | ❌ |
| 质检员 | ✅ | ❌ | ❌ | ❌ |

**权限实现**
```typescript
// 后端权限检查
router.get('/api/week-view/:year/:week',
  authMiddleware,
  checkPermission('weekview:view'),
  getWeekView
);

router.put('/api/week-view/order/:id',
  authMiddleware,
  checkPermission('weekview:edit'),
  updateOrderSchedule
);

// 前端权限控制
const { hasPermission } = usePermission();

{hasPermission('weekview:edit') && (
  <Button onClick={handleDragEnable}>启用拖拽排程</Button>
)}
```

---

## 开发计划

本开发计划分为**两个优先级**，优先级1是核心交付，优先级2是可选增强。

---

## 【优先级 1：核心交付】必须完成（13-19天）

### 阶段一：低代码引擎开发（5-7天）

**第1步：Schema 配置管理（2天）**
- 集成 Monaco Editor，支持 JSON 编辑
- 实现 Schema CRUD API（`/api/schemas`）
- Schema 列表展示（左侧侧边栏）
- Schema 创建、编辑、删除功能
- Schema 格式验证（JSON Schema 校验）

**第2步：动态渲染引擎（3-5天）**
- 开发 DynamicForm 组件
  - 支持 5 种字段类型：text、number、date、select、textarea
  - 自动表单验证（必填项、类型校验）
  - 表单布局配置（horizontal/vertical）
- 开发 DynamicTable 组件
  - 根据 Schema 渲染表格
  - 支持分页、排序、筛选
  - 支持 CRUD 按钮（创建、编辑、删除、查看）
- 实现通用 CRUD API（`/api/data/:entity`）
  - 支持任意实体的增删改查
  - 支持查询参数（分页、排序、筛选）

**验收标准：**
- ✅ 能通过 Schema 配置生成完整的 CRUD 页面
- ✅ 表单和表格正确渲染
- ✅ CRUD 操作功能完整
- ✅ 数据验证正常工作

---

### 阶段二：平台基础设施（4-6天）

**第3步：认证授权系统（2-3天）**
- 后端
  - 实现 JWT 认证中间件
  - 实现用户登录/登出 API
  - 密码 Bcrypt 加密
  - Token 自动刷新机制
- 前端
  - 实现登录页面
  - 封装 Axios 拦截器（自动携带 Token）
  - 实现路由守卫（未登录跳转登录页）

**第4步：权限管理系统（2-3天）**
- 后端
  - 用户管理 CRUD API
  - 角色管理 CRUD API
  - 权限检查中间件（`checkPermission`）
  - 初始化种子数据（预置 5 种角色）
- 前端
  - 用户管理页面（创建、编辑、禁用用户）
  - 角色管理页面（创建、编辑角色，配置权限）
  - 实现 `usePermission` Hook
  - 按钮级权限控制

**验收标准：**
- ✅ 用户能登录并获取 Token
- ✅ 能创建用户并分配角色
- ✅ 不同角色登录后看到的菜单和按钮不同
- ✅ 无权限用户访问受保护页面/API 会被拦截

---

### 阶段三：MES 示例应用开发（4-6天）

**第5步：配置 6 个 MES 页面的 Schema（1-2天）**

创建以下 6 个 Schema 配置文件：

1. **工单管理 Schema**
   ```typescript
   {
     id: 'work-order',
     name: '工单管理',
     entity: 'WorkOrder',
     fields: [
       { key: 'orderNo', label: '工单号', type: 'text', required: true },
       { key: 'productName', label: '产品名称', type: 'text', required: true },
       { key: 'quantity', label: '数量', type: 'number', required: true },
       { key: 'status', label: '状态', type: 'select', options: [...] },
       { key: 'priority', label: '优先级', type: 'select', options: [...] },
       { key: 'planStartDate', label: '计划开始', type: 'date' },
       { key: 'planEndDate', label: '计划结束', type: 'date' }
     ]
   }
   ```

2. **报工记录 Schema**
3. **质检记录 Schema**
4. **设备管理 Schema**
5. **用户管理 Schema**
6. **角色管理 Schema**

**第6步：数据导入导出功能（1-2天）**
- 后端
  - 使用 ExcelJS 实现导出 API
  - 使用 Multer + XLSX 实现导入 API
  - 提供 Excel 模板下载
- 前端
  - 添加导出按钮（在表格工具栏）
  - 添加导入按钮（上传 Excel）
  - 显示导入结果（成功/失败统计）

**第7步：测试和优化（2天）**
- 测试 6 个页面的动态渲染
- 测试数据关联（工单 → 报工/质检）
- 测试权限控制（不同角色访问不同页面）
- 测试导入导出功能
- 修复 Bug，优化用户体验

**验收标准：**
- ✅ 6 个页面完全通过 Schema 配置生成
- ✅ CRUD 功能完整
- ✅ 数据关联正确（工单 → 报工、质检）
- ✅ 权限控制生效
- ✅ 导入导出功能正常

---

**【优先级 1 总结】**

完成以上 3 个阶段后，你将拥有：
1. ✅ 完整的低代码平台引擎
2. ✅ 完善的认证授权和权限管理系统
3. ✅ 6 个通过低代码生成的 MES 页面
4. ✅ 数据导入导出功能

**此时已经是一个完整可交付的系统！**

---

## 【优先级 2：增强功能】可选实现（6-8天）

如果时间充裕，可以继续开发以下高级可视化页面：

### 阶段四：实时看板开发（3-4天）

**功能点**
1. 数据统计 API
   - 今日工单数、完成率
   - 质检合格率
   - 设备运行率
   - 工单状态分布
   - 质量趋势（最近7天）

2. 看板页面布局
   - 顶部数字卡片（4个核心指标）
   - 中间图表区域（饼图、折线图）
   - 设备状态矩阵
   - 最近报工记录表格

3. 实时更新机制
   - 手动刷新按钮
   - 定时自动刷新（5秒）

**技术实现**
- 使用 Recharts 或 ECharts 图表库
- 使用 Ant Design Card 和 Statistic 组件

**验收标准**
- 所有统计数据准确
- 图表正常显示
- 数据能自动刷新
- 页面布局美观

---

### 阶段五：周视图开发（3-4天）

**功能点**
1. 周数据查询 API
   - 获取指定周的工单列表
   - 计算工单进度
   - 计算设备负载
   - 周统计数据汇总

2. 三种视图模式
   - 甘特图视图（推荐：CSS Grid 自实现）
   - 设备日历视图
   - 工单列表视图

3. 周导航和交互
   - 上一周/下一周切换
   - 日期选择器跳转
   - 视图模式切换
   - 手动刷新

**技术实现**
```typescript
// 简化的甘特图实现（CSS Grid）
const SimpleGantt = ({ orders, equipments, weekDates }) => {
  return (
    <div className="gantt-container">
      <div className="gantt-header">
        {weekDates.map(date => <div>{formatDate(date)}</div>)}
      </div>
      {equipments.map(equipment => (
        <div className="gantt-row">
          <div className="equipment-label">{equipment.name}</div>
          <div className="timeline">
            {/* 工单块 */}
          </div>
        </div>
      ))}
    </div>
  );
};
```

**验收标准**
- 甘特图正确显示工单排程
- 工单进度计算准确
- 设备负载显示正确
- 三种视图模式切换流畅

---

**【优先级 2 总结】**

完成这两个阶段后，系统将增加：
1. ✅ 实时数据看板（可视化展示）
2. ✅ 周视图排程（甘特图）

这些是锦上添花的功能，让 MES 系统更加完善和专业。

---

## 开发时间总结

| 优先级 | 阶段 | 功能 | 工期 |
|--------|------|------|------|
| **优先级 1** | 阶段一 | 低代码引擎 | 5-7天 |
| **优先级 1** | 阶段二 | 平台基础设施 | 4-6天 |
| **优先级 1** | 阶段三 | MES 示例应用 | 4-6天 |
| **小计** | - | **核心交付** | **13-19天** ✅ |
| **优先级 2** | 阶段四 | 实时看板 | 3-4天 |
| **优先级 2** | 阶段五 | 周视图 | 3-4天 |
| **小计** | - | **增强功能** | **6-8天** |
| **总计** | - | **完整版本** | **19-27天** |

---

## 开发建议

1. **先做优先级 1**：确保核心功能完整，可以交付一个完整可用的系统
2. **评估时间**：如果时间紧张，优先级 1 完成后就可以交付
3. **再做优先级 2**：如果时间充裕，继续开发实时看板和周视图
4. **灵活调整**：根据实际进度灵活调整，保证核心功能质量

---

**功能点**
1. 集成 Monaco Editor，支持 JSON 编辑
2. Schema 列表展示（左侧侧边栏）
3. Schema 创建、编辑、删除功能
4. Schema 验证（JSON Schema 校验）
5. 预置 MES 三个页面的 Schema 模板

**技术实现**
```typescript
// 示例：工单 Schema 模板
const workOrderSchema: PageSchema = {
  id: 'work-order',
  name: '工单管理',
  entity: 'WorkOrder',
  fields: [
    { key: 'orderNo', label: '工单号', type: 'text', required: true },
    { key: 'productName', label: '产品名称', type: 'text', required: true },
    { key: 'quantity', label: '数量', type: 'number', required: true },
    {
      key: 'status',
      label: '状态',
      type: 'select',
      options: [
        { label: '待生产', value: 'pending' },
        { label: '生产中', value: 'in_progress' },
        { label: '已完成', value: 'completed' }
      ]
    }
  ],
  listConfig: {
    columns: [
      { key: 'orderNo', title: '工单号', width: 150 },
      { key: 'productName', title: '产品名称' },
      { key: 'quantity', title: '数量', width: 100 },
      { key: 'status', title: '状态', width: 120 }
    ],
    actions: ['create', 'edit', 'delete', 'view']
  },
  formConfig: {
    layout: 'horizontal',
    labelCol: { span: 6 },
    wrapperCol: { span: 14 }
  }
};
```

**验收标准**
- 能创建和编辑 Schema
- 能加载和保存 Schema 到后端
- JSON 编辑器有语法高亮和错误提示

---

### 阶段四：运行时页面开发（3-4天）

**功能点**
1. 根据 Schema 动态渲染表格（DynamicTable 组件）
2. 根据 Schema 动态渲染表单（DynamicForm 组件）
3. 实现增删改查操作
4. 数据联动（如：创建报工时选择工单）
5. 简单的数据统计（如：工单完成率、质检合格率）

**核心组件实现**

```typescript
// DynamicTable.tsx
interface DynamicTableProps {
  schema: PageSchema;
  dataSource: any[];
  loading: boolean;
  onEdit: (record: any) => void;
  onDelete: (id: string) => void;
  onCreate: () => void;
}

// DynamicForm.tsx
interface DynamicFormProps {
  schema: PageSchema;
  initialValues?: any;
  onSubmit: (values: any) => Promise<void>;
  onCancel: () => void;
}
```

**验收标准**
- 能根据不同 Schema 正确渲染页面
- CRUD 操作功能完整
- 表单验证正常工作

---

### 阶段五：MES 基础业务实现（2-3天）

**功能点**
1. 工单管理
   - 创建工单（工单号、产品名称、数量、指定设备）
   - 查看工单列表
   - 更新工单状态
   - 查看关联的报工和质检记录

2. 报工记录
   - 选择工单进行报工
   - 记录工时、完成数量
   - 按工单统计总工时

3. 质检记录
   - 选择工单进行质检
   - 记录质检结果（合格/不合格）、检验数量、合格数量
   - 统计质检合格率

**业务流程**
```
创建工单 → 员工报工 → 质检检验 → 工单完成
```

**验收标准**
- 三个页面功能完整
- 数据关联正确（外键约束）
- 统计数据准确

---

### 阶段六：设备管理功能（2-3天）

**功能点**
1. 设备档案管理
   - CRUD 操作（设备编号、名称、类别、型号）
   - 设备状态实时更新（idle, running, maintenance, fault）
   - 设备详情页（显示当前绑定的工单）

2. 设备维护管理
   - 创建维护记录（计划性维护 / 故障维修）
   - 维护记录列表
   - 记录维护时间、技术人员、费用、结果

3. 设备状态日志
   - 记录设备状态变更历史
   - 提供给看板使用

**技术实现**
```typescript
// 设备状态更新时自动记录日志
const updateEquipmentStatus = async (equipmentId: string, newStatus: string) => {
  // 更新设备状态
  await prisma.equipment.update({
    where: { id: equipmentId },
    data: { status: newStatus }
  });

  // 记录状态日志
  await prisma.equipmentStatusLog.create({
    data: {
      equipmentId,
      status: newStatus,
      timestamp: new Date()
    }
  });
};
```

**验收标准**
- 设备 CRUD 功能完整
- 维护记录能正确关联设备
- 状态变更能自动记录日志

---

### 阶段七：实时看板开发（3-4天）

**功能点**
1. 数据统计 API
   - 今日工单数、完成率
   - 质检合格率
   - 设备运行率
   - 工单状态分布
   - 质量趋势（最近7天）

2. 看板页面布局
   - 顶部数字卡片（4个核心指标）
   - 中间图表区域（饼图、折线图）
   - 设备状态矩阵
   - 最近报工记录表格

3. 实时更新机制
   - 方案选择：轮询（5秒）或 WebSocket
   - 建议使用轮询（简单易实现）

**技术实现**
```typescript
// 前端：使用 Recharts 绘制图表
import { PieChart, Pie, LineChart, Line } from 'recharts';

// 数据自动刷新
useEffect(() => {
  const fetchData = async () => {
    const data = await dashboardService.getStatistics();
    setDashboardData(data);
  };

  fetchData();
  const interval = setInterval(fetchData, 5000);

  return () => clearInterval(interval);
}, []);
```

**验收标准**
- 所有统计数据准确
- 图表正常显示
- 数据能自动刷新
- 页面布局美观

---

### 阶段八：数据导入导出功能（2天）

**功能点**
1. 数据导出
   - 工单列表导出为 Excel
   - 报工记录导出
   - 质检记录导出
   - 支持导出当前页/全部数据/筛选结果

2. 数据导入
   - 下载 Excel 模板
   - 上传 Excel 文件批量导入工单
   - 数据验证（必填项、格式检查）
   - 返回导入结果（成功/失败统计）

**技术实现**
```typescript
// 前端：使用 xlsx 库导出
import * as XLSX from 'xlsx';

const handleExport = () => {
  const ws = XLSX.utils.json_to_sheet(dataSource);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, 'Sheet1');
  XLSX.writeFile(wb, '工单列表.xlsx');
};

// 后端：使用 exceljs + multer 处理导入
router.post('/api/import/workorders',
  authMiddleware,
  checkPermission('workorder:create'),
  upload.single('file'),
  importWorkOrders
);
```

**验收标准**
- Excel 导出功能正常，格式美观
- Excel 导入成功，错误能正确提示
- 模板下载功能正常
- 导入结果统计准确

---

### 阶段九：周视图开发（3-4天）

**功能点**
1. 周数据查询 API
   - 获取指定周的工单列表
   - 计算工单进度
   - 计算设备负载
   - 周统计数据汇总

2. 甘特图视图
   - 集成 react-gantt-chart 或使用自定义实现
   - 工单块渲染（按设备分组）
   - 颜色编码（按状态）
   - 进度显示
   - Tooltip 悬停提示

3. 设备日历视图
   - 7列网格布局（周一到周日）
   - 设备行展示
   - 工单占用显示
   - 负载百分比显示

4. 工单列表视图
   - 表格展示本周工单
   - 筛选和排序功能
   - 快速查看详情

5. 周导航和交互
   - 上一周/下一周切换
   - 日期选择器跳转
   - 视图模式切换
   - 手动刷新
   - 定时自动刷新（60秒）

**技术实现**
```typescript
// 前端：周视图页面结构
const WeekViewPage = () => {
  const [currentWeek, setCurrentWeek] = useState(getCurrentWeek());
  const [viewMode, setViewMode] = useState<'gantt' | 'calendar' | 'list'>('gantt');
  const { data, loading, refetch } = useWeekData(currentWeek.year, currentWeek.week);

  return (
    <div className="week-view-container">
      {/* 头部：周选择器 + 视图切换 */}
      <div className="week-header">
        <WeekSelector
          value={currentWeek}
          onChange={setCurrentWeek}
        />
        <ViewModeSwitcher
          value={viewMode}
          onChange={setViewMode}
        />
        <Button onClick={refetch}>刷新</Button>
      </div>

      {/* 主体：根据视图模式渲染 */}
      {viewMode === 'gantt' && (
        <GanttView
          orders={data.orders}
          equipments={data.equipments}
          weekStart={data.summary.startDate}
        />
      )}

      {viewMode === 'calendar' && (
        <CalendarView
          orders={data.orders}
          equipments={data.equipments}
          weekDates={getWeekDates(data.summary.startDate)}
        />
      )}

      {viewMode === 'list' && (
        <ListView
          orders={data.orders}
        />
      )}

      {/* 底部：周统计摘要 */}
      <WeekSummary summary={data.summary} />
    </div>
  );
};
```

**验收标准**
- 甘特图正确显示工单排程
- 工单进度计算准确
- 设备负载显示正确
- 三种视图模式切换流畅
- 周导航功能正常
- 统计数据准确
- 权限控制生效

---

### 阶段十：优化与测试（2-3天）

**优化点**
1. 错误处理和用户提示
2. Loading 状态优化
3. 表单体验优化（防抖、节流）
4. 代码重构和注释完善
5. 响应式布局调整
6. 性能优化（大数据列表虚拟滚动）

**测试清单**
- [ ] 用户登录和权限控制测试
- [ ] Schema CRUD 功能测试
- [ ] 动态渲染正确性测试
- [ ] MES 业务流程测试（工单 → 报工 → 质检）
- [ ] 设备管理功能测试
- [ ] 实时看板数据准确性测试
- [ ] 周视图功能测试（甘特图、日历、列表视图）
- [ ] 周视图数据计算准确性测试（进度、负载、统计）
- [ ] 数据导入导出功能测试
- [ ] 边界情况测试（空数据、错误输入、超长文本）
- [ ] 权限测试（不同角色功能隔离）
- [ ] 性能测试（大量工单渲染）
- [ ] 浏览器兼容性测试

**开发周期总计：21-29 天**

---

## API 接口设计

### Schema 管理 API

```
GET    /api/schemas           # 获取所有 Schema
GET    /api/schemas/:id       # 获取单个 Schema
POST   /api/schemas           # 创建 Schema
PUT    /api/schemas/:id       # 更新 Schema
DELETE /api/schemas/:id       # 删除 Schema
```

### 动态数据 API

```
GET    /api/data/:entity            # 查询列表（支持分页、排序、筛选）
GET    /api/data/:entity/:id        # 查询单条
POST   /api/data/:entity            # 创建
PUT    /api/data/:entity/:id        # 更新
DELETE /api/data/:entity/:id        # 删除
```

**查询参数示例**
```
GET /api/data/WorkOrder?page=1&pageSize=10&status=pending&orderBy=createdAt:desc
```

### MES 统计 API

```
GET /api/stats/work-orders          # 工单统计
GET /api/stats/quality-rate         # 质检合格率
```

### 用户认证 API

```
POST   /api/auth/login              # 用户登录
POST   /api/auth/logout             # 用户登出
GET    /api/auth/me                 # 获取当前用户信息
POST   /api/auth/refresh            # 刷新 Token
```

**登录请求示例**
```json
POST /api/auth/login
{
  "username": "admin",
  "password": "123456"
}

// 响应
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": "xxx",
    "username": "admin",
    "realName": "管理员",
    "role": {
      "id": "xxx",
      "name": "admin",
      "permissions": ["*"]
    }
  }
}
```

### 用户管理 API

```
GET    /api/users                   # 获取用户列表
GET    /api/users/:id               # 获取单个用户
POST   /api/users                   # 创建用户
PUT    /api/users/:id               # 更新用户
DELETE /api/users/:id               # 删除用户
PUT    /api/users/:id/status        # 启用/禁用用户
```

**权限要求：** `user:view`, `user:create`, `user:edit`, `user:delete`

### 角色管理 API

```
GET    /api/roles                   # 获取角色列表
GET    /api/roles/:id               # 获取单个角色
POST   /api/roles                   # 创建角色
PUT    /api/roles/:id               # 更新角色
DELETE /api/roles/:id               # 删除角色
```

**权限要求：** `role:view`, `role:create`, `role:edit`, `role:delete`

### 设备管理 API

```
GET    /api/equipment               # 获取设备列表
GET    /api/equipment/:id           # 获取单个设备
POST   /api/equipment               # 创建设备
PUT    /api/equipment/:id           # 更新设备
DELETE /api/equipment/:id           # 删除设备
PUT    /api/equipment/:id/status    # 更新设备状态
```

**权限要求：** `equipment:view`, `equipment:create`, `equipment:edit`, `equipment:delete`

### 设备维护 API

```
GET    /api/maintenance             # 获取维护记录列表
GET    /api/maintenance/:id         # 获取单个维护记录
POST   /api/maintenance             # 创建维护记录
PUT    /api/maintenance/:id         # 更新维护记录
DELETE /api/maintenance/:id         # 删除维护记录
GET    /api/maintenance/equipment/:equipmentId  # 获取指定设备的维护记录
```

### 实时看板 API

```
GET    /api/dashboard/overview      # 获取概览数据（4个核心指标）
GET    /api/dashboard/order-status  # 获取工单状态分布
GET    /api/dashboard/quality-trend # 获取质量趋势（最近7天）
GET    /api/dashboard/equipment-status  # 获取设备状态分布
GET    /api/dashboard/recent-reports    # 获取最近报工记录
```

**响应示例（概览数据）**
```json
GET /api/dashboard/overview
{
  "todayOrders": 25,
  "orderCompletionRate": 68.5,
  "qualityPassRate": 97.8,
  "equipmentRunningRate": 75.0
}
```

**权限要求：** `dashboard:view`

### 数据导入导出 API

```
GET    /api/export/workorders       # 导出工单列表（Excel）
GET    /api/export/workreports      # 导出报工记录（Excel）
GET    /api/export/qualitychecks    # 导出质检记录（Excel）
POST   /api/import/workorders       # 导入工单（Excel）
GET    /api/template/workorders     # 下载工单导入模板
```

**导出请求示例**
```
GET /api/export/workorders?status=pending&format=xlsx

响应：
Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
Content-Disposition: attachment; filename=workorders_20250119.xlsx
```

**导入请求示例**
```
POST /api/import/workorders
Content-Type: multipart/form-data

file: workorders.xlsx

// 响应
{
  "success": 45,
  "failed": 3,
  "errors": [
    {
      "row": 12,
      "error": "工单号已存在"
    }
  ]
}
```

### 周视图 API

```
GET    /api/week-view/:year/:week   # 获取指定周的数据（工单、设备、统计）
GET    /api/week-view/current        # 获取当前周的数据
PUT    /api/week-view/order/:id      # 更新工单排程（调整时间）
GET    /api/week-view/equipment-load/:equipmentId/:date  # 获取设备某天的负载
```

**获取周数据请求示例**
```
GET /api/week-view/2025/3

// 响应
{
  "summary": {
    "weekNumber": 3,
    "startDate": "2025-01-13T00:00:00.000Z",
    "endDate": "2025-01-19T23:59:59.999Z",
    "totalOrders": 15,
    "completedOrders": 8,
    "inProgressOrders": 5,
    "pendingOrders": 2,
    "avgCompletion": 53.5,
    "equipmentUtilization": 75.2,
    "delayedOrders": 2
  },
  "orders": [
    {
      "id": "xxx",
      "orderNo": "WO-001",
      "productName": "iPhone外壳",
      "quantity": 1000,
      "status": "in_progress",
      "priority": "high",
      "planStartDate": "2025-01-13T08:00:00.000Z",
      "planEndDate": "2025-01-15T18:00:00.000Z",
      "actualStartDate": "2025-01-13T08:30:00.000Z",
      "progress": 60,
      "isDelayed": false,
      "equipment": {
        "id": "xxx",
        "equipmentNo": "EQ-001",
        "equipmentName": "冲压机A",
        "status": "running"
      }
    }
    // ... 更多工单
  ],
  "equipments": [
    {
      "id": "xxx",
      "equipmentNo": "EQ-001",
      "equipmentName": "冲压机A",
      "status": "running",
      "dailyLoads": [
        { "date": "2025-01-13", "load": 100 },
        { "date": "2025-01-14", "load": 85 },
        { "date": "2025-01-15", "load": 60 },
        { "date": "2025-01-16", "load": 80 },
        { "date": "2025-01-17", "load": 0 },
        { "date": "2025-01-18", "load": 0 },
        { "date": "2025-01-19", "load": 0 }
      ]
    }
    // ... 更多设备
  ]
}
```

**更新工单排程请求示例**
```
PUT /api/week-view/order/xxx
{
  "planStartDate": "2025-01-14T08:00:00.000Z",
  "planEndDate": "2025-01-16T18:00:00.000Z",
  "equipmentId": "yyy"  // 可选，更换设备
}

// 响应
{
  "success": true,
  "message": "工单排程已更新",
  "order": {
    // 更新后的工单数据
  }
}
```

**权限要求：** `weekview:view`, `weekview:edit`

---

## AI 使用方式说明

### 开发过程中的 AI 应用

**代码生成（约占 60%）**
1. 使用 Claude Code 生成 Prisma Schema 定义
2. 使用 AI 生成 Express 路由和控制器模板
3. 使用 AI 生成 React 组件基础结构
4. 使用 AI 生成 TypeScript 类型定义

**代码优化与重构（约占 20%）**
1. 使用 AI 审查代码，提出优化建议
2. 使用 AI 重构复杂函数，提高可读性
3. 使用 AI 添加错误处理和边界情况处理

**问题解决（约占 15%）**
1. 调试时询问 AI 可能的错误原因
2. 咨询 AI 最佳实践和设计模式
3. 学习新的 API 用法（如 Prisma、Ant Design）

**文档编写（约占 5%）**
1. 使用 AI 生成项目文档结构
2. 使用 AI 生成代码注释
3. 使用 AI 整理开发日志

### AI 使用记录模板

开发报告中应包含以下内容：

```markdown
## AI 使用记录

### 第一阶段：项目初始化
- **使用场景**：生成 Prisma Schema
- **AI 工具**：Claude Code
- **提示词示例**：
  "帮我设计一个 MES 系统的数据库 Schema，包含工单、报工、质检三个实体..."
- **生成结果**：完整的 Prisma Schema 定义
- **人工修改**：调整了字段类型和关系定义

### 第二阶段：API 开发
...
```

---

## 技术难点与解决方案

### 1. 动态表单渲染

**难点**：根据不同的字段类型动态渲染表单控件

**解决方案**：
```typescript
const renderFormItem = (field: FieldConfig) => {
  switch (field.type) {
    case 'text':
      return <Input />;
    case 'number':
      return <InputNumber />;
    case 'select':
      return <Select options={field.options} />;
    case 'date':
      return <DatePicker />;
    case 'textarea':
      return <Input.TextArea />;
    default:
      return <Input />;
  }
};
```

### 2. 通用 CRUD API

**难点**：单个 API 支持不同实体的 CRUD 操作

**解决方案**：
```typescript
// 使用 Prisma 的动态模型访问
app.get('/api/data/:entity', async (req, res) => {
  const { entity } = req.params;
  const model = prisma[entity as keyof typeof prisma];
  const data = await (model as any).findMany();
  res.json(data);
});
```

### 3. Schema 验证

**难点**：确保用户输入的 Schema 格式正确

**解决方案**：
- 使用 Zod 或 JSON Schema 进行验证
- 在保存前进行格式校验
- 提供清晰的错误提示

---

## 交付物清单

1. [ ] 前端源代码
   - 所有组件（DynamicTable, DynamicForm, Dashboard 等）
   - 所有页面（登录、用户管理、角色管理、工单、报工、质检、设备、看板等）
   - 路由配置和权限控制

2. [ ] 后端源代码
   - API 路由（认证、用户、角色、设备、看板、导入导出等）
   - 中间件（认证、权限检查）
   - 数据库配置（Prisma Schema）

3. [ ] 数据库文件
   - Prisma Schema 定义
   - 种子数据脚本（初始角色和管理员用户）
   - SQLite 数据库文件（可选）

4. [ ] 项目文档
   - README.md（项目说明、功能列表、运行指南）
   - 开发文档.md（本文档，含架构设计和开发计划）
   - AI使用报告.md（详细记录 AI 使用情况和贡献）
   - API文档（可选，可使用 Swagger）

5. [ ] 演示材料
   - 功能演示视频或 GIF
   - 核心功能截图（登录、看板、配置页面等）
   - 测试用例和测试结果

6. [ ] 额外交付物
   - Excel 导入模板文件
   - 预置的 Schema 配置示例（JSON 文件）
   - 部署文档（可选）

---

## 环境要求

**开发环境**
- Node.js >= 18.x
- npm >= 9.x
- VS Code（推荐）

**推荐 VS Code 插件**
- Prisma
- ESLint
- Prettier
- TypeScript Vue Plugin (Volar)

---

## 启动指南

### 后端启动

```bash
cd backend
npm install
npx prisma generate
npx prisma migrate dev
npm run dev
```

### 前端启动

```bash
cd frontend
npm install
npm run dev
```

访问 `http://localhost:5173` 查看应用。

---

## 评分标准参考

假设这是软件工程作业，可能的评分点：

1. **功能完整性（30%）**
   - 用户登录和权限管理完整
   - Schema 配置功能完整
   - 动态渲染正确
   - MES 业务流程完整（工单、报工、质检、设备）
   - 实时看板功能正常
   - 数据导入导出功能完整

2. **代码质量（25%）**
   - TypeScript 类型定义完整
   - 代码结构清晰、模块化
   - 注释充分
   - 错误处理完善
   - 安全性考虑（密码加密、SQL 注入防护）

3. **AI 使用（20%）**
   - AI 使用记录详细
   - 合理利用 AI 提高效率
   - 对 AI 生成代码有审查和优化
   - AI 辅助解决复杂问题的案例

4. **设计合理性（15%）**
   - 架构设计合理
   - 数据库设计规范（RBAC 权限模型）
   - API 设计符合 RESTful 规范
   - 权限控制设计合理

5. **文档完整性（10%）**
   - 文档结构清晰
   - 说明详细
   - 有使用示例
   - AI 使用报告完整

---

## 备注

1. 本项目已实现的功能：
   - ✅ 用户认证和授权（JWT + RBAC）
   - ✅ 数据验证和安全防护（Bcrypt 密码加密）
   - ✅ 权限控制（前后端双重校验）
   - ✅ 数据导入导出
   - ✅ 实时看板

2. 生产环境建议增加：
   - 更完善的错误处理和日志记录
   - 单元测试和集成测试
   - API 限流和防护
   - HTTPS 加密传输
   - 数据库备份策略
   - 性能监控

3. 可扩展方向：
   - 支持更多字段类型（富文本、文件上传、图片等）
   - 支持复杂布局（多列、分组、tabs、步骤条）
   - 支持自定义校验规则和计算字段
   - 支持工作流引擎（工单审批流程）
   - 支持移动端（响应式布局或独立 App）
   - 支持多租户（SaaS 模式）

4. Schema 设计可以参考现有低代码平台：
   - Amis（百度开源）
   - FormRender（阿里开源）
   - X-Render（阿里开源）

5. 依赖包版本建议：
   ```json
   {
     "react": "^18.2.0",
     "antd": "^5.12.0",
     "recharts": "^2.10.0",
     "axios": "^1.6.0",
     "react-router-dom": "^6.20.0",
     "xlsx": "^0.18.5",
     "express": "^4.18.0",
     "prisma": "^5.7.0",
     "jsonwebtoken": "^9.0.0",
     "bcrypt": "^5.1.0",
     "exceljs": "^4.4.0",
     "multer": "^1.4.0"
   }
   ```


     1. ✅ 正常显示系统列表（无需登录）
  2. ✅ 选择"示例MES系统"后弹出登录页面（因为该系统启用了认证）
  3. ✅ 使用以下账号登录测试：
    - 超级管理员：admin / admin123
    - 系统管理员：sysadmin / sysadmin123
    - 组长（普通用户）：leader / user123
    - 工人（只读用户）：worker / worker123
