# MSE 系统 - 快速启动指南（发布版）

## 📋 环境要求

- **Node.js**: >= 18.0.0
- **npm**: >= 9.0.0
- **操作系统**: Windows / macOS / Linux

## 📦 数据库说明

本项目已包含预配置的 SQLite 数据库（`backend/prisma/dev.db`），内含：
- ✅ 默认管理员账号：`admin` / `admin123`
- ✅ 示例系统配置和数据
- ✅ MES 系统相关表结构

**您可以直接使用现有数据，或选择重新初始化：**

### 使用现有数据库（推荐）
直接启动即可，无需额外操作。

### 重新初始化数据库
如果需要清空数据重新开始：
```bash
cd backend
# 删除现有数据库
Remove-Item prisma\dev.db  # Windows
# rm prisma/dev.db         # macOS/Linux

# 重新生成数据库和初始数据
npx prisma generate
npx prisma db push
npm run seed
```

## 🚀 快速开始

### 1️⃣ 后端启动

```bash
# 进入后端目录
cd backend

# 安装依赖
npm install

# 复制环境变量文件（如果不存在）
# Windows PowerShell:
Copy-Item .env.example .env
# macOS/Linux:
# cp .env.example .env

# 生成 Prisma 客户端
npx prisma generate

# 【可选】初始化数据库（如果压缩包已包含 dev.db，可跳过此步）
# npm run seed

# 启动后端服务（开发模式）
npm run dev
```

后端服务将运行在 `http://localhost:3000`

### 2️⃣ 前端启动

**新开一个终端窗口**，执行：

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 复制环境变量文件（如果不存在）
# Windows PowerShell:
Copy-Item .env.example .env
# macOS/Linux:
# cp .env.example .env

# 启动前端服务
npm run dev
```

前端服务将运行在 `http://localhost:5173`

### 3️⃣ 访问系统

打开浏览器访问：`http://localhost:5173`

默认管理员账号：
- **用户名**: `admin`
- **密码**: `admin123`

## 📝 常见问题

### ❌ 端口被占用

如果 3000 或 5173 端口被占用，可以修改：
- 后端：编辑 `backend/.env` 中的 `PORT`
- 前端：Vite 会自动使用下一个可用端口，或在 `frontend/vite.config.ts` 中配置

### ❌ 数据库相关错误

```bash
# 重新生成 Prisma 客户端
cd backend
npx prisma generate

# 查看数据库（可选）
npx prisma studio
```

### ❌ 依赖安装失败

尝试清除缓存后重新安装：

```bash
# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json
# 或 Windows PowerShell:
# Remove-Item -Recurse -Force node_modules, package-lock.json

# 重新安装
npm install
```

### ❌ 前端无法连接后端

检查：
1. 后端是否正常运行（访问 `http://localhost:3000/health` 应返回 `{"status":"ok"}`）
2. 前端 `.env` 文件中的 `VITE_API_BASE_URL` 是否正确

## 🔧 生产环境部署

### 后端构建

```bash
cd backend
npm run build
npm run start
```

### 前端构建

```bash
cd frontend
npm run build
# 构建产物在 dist/ 目录
```

**⚠️ 生产环境安全提示**：
- 必须修改 `backend/.env` 中的 `JWT_SECRET` 为强随机密钥
- 配置合适的 CORS 策略（不要使用 `origin: "*"`）
- 使用 HTTPS 和反向代理（如 Nginx）

## 📚 其他说明

- **生成演示数据**（可选）：`cd backend && npm run demo-data`
- **数据库管理界面**：`cd backend && npx prisma studio`
- **查看所有可用命令**：查看 `backend/package.json` 和 `frontend/package.json` 中的 `scripts` 部分

---

如有问题，请查看项目文档或联系开发团队。
