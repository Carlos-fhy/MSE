# 数据库导出与导入说明

## 📊 当前数据库内容

本 SQLite 数据库（`backend/prisma/dev.db`）包含：

### 系统数据
- 预定义的 MES 系统配置
- 低代码 Schema 模板
- 用户角色与权限配置

### 默认账号
- **超级管理员**: `admin` / `admin123`
- **其他测试账号**: 请查看数据库或运行 `npx prisma studio` 查看

## 🔄 数据库操作指南

### 方式一：使用现有数据库（已包含在压缩包中）

1. 解压后直接运行即可
2. 数据库文件位置：`backend/prisma/dev.db`
3. 包含所有预配置数据

### 方式二：重新初始化空数据库

```bash
cd backend

# 删除现有数据库
Remove-Item prisma\dev.db -Force  # Windows
# rm prisma/dev.db                # macOS/Linux

# 推送 schema 到数据库（创建表结构）
npx prisma db push

# 运行 seed 脚本（创建初始数据）
npm run seed
```

### 方式三：生成演示数据

```bash
cd backend
npm run demo-data
```

这将生成：
- 工单数据
- 报工记录
- 质检记录
- 设备信息
- 维护记录

## 🛠️ 数据库管理工具

### Prisma Studio（推荐）

```bash
cd backend
npx prisma studio
```

打开浏览器访问 `http://localhost:5555`，可以：
- 查看所有表和数据
- 添加/编辑/删除记录
- 可视化数据关系

### SQLite 客户端工具

可选的第三方工具：
- **DB Browser for SQLite**: https://sqlitebrowser.org/
- **DBeaver**: https://dbeaver.io/
- **TablePlus**: https://tableplus.com/

## 📤 导出数据（备份）

### 方式一：直接复制数据库文件

```bash
# 创建备份
Copy-Item backend\prisma\dev.db backend\prisma\dev.db.backup  # Windows
# cp backend/prisma/dev.db backend/prisma/dev.db.backup       # macOS/Linux
```

### 方式二：使用 Prisma Studio 导出

1. 运行 `npx prisma studio`
2. 选择表 → 导出为 CSV/JSON

## 📥 导入数据

### 从备份恢复

```bash
# 恢复备份
Copy-Item backend\prisma\dev.db.backup backend\prisma\dev.db -Force  # Windows
# cp backend/prisma/dev.db.backup backend/prisma/dev.db               # macOS/Linux
```

### 从 Excel 导入（系统内置功能）

系统支持从 Excel 导入数据：
1. 登录系统
2. 进入相应的数据管理页面
3. 使用"导入"功能上传 Excel 文件

## ⚠️ 注意事项

1. **SQLite 限制**：
   - 单文件数据库，不适合大规模并发
   - 生产环境建议迁移到 PostgreSQL/MySQL

2. **数据安全**：
   - 定期备份数据库文件
   - 不要将包含敏感数据的数据库文件公开分享

3. **迁移到生产数据库**：
   
   修改 `backend/.env`：
   ```env
   # PostgreSQL 示例
   DATABASE_URL="postgresql://user:password@localhost:5432/mes_db"
   
   # MySQL 示例
   DATABASE_URL="mysql://user:password@localhost:3306/mes_db"
   ```
   
   然后运行：
   ```bash
   npx prisma db push
   npm run seed
   ```

## 🔍 查看数据库结构

```bash
cd backend
npx prisma studio
# 或查看 schema 文件
cat prisma\schema.prisma  # Windows
# cat prisma/schema.prisma # macOS/Linux
```

---

更多信息请参考 [Prisma 官方文档](https://www.prisma.io/docs/)
