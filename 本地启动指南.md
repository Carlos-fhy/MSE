# 项目本地启动指南

## 📋 前置条件检查

在启动项目之前，请确保你的电脑已安装以下软件：

### 必需软件
- **Node.js** >= 18.x
- **npm** >= 9.x（随 Node.js 自动安装）

### 验证安装
打开终端（命令行），运行以下命令检查：

```bash
node -v    # 应显示 v18.x.x 或更高版本
npm -v     # 应显示 9.x.x 或更高版本
```

如果没有安装，请访问 https://nodejs.org/ 下载安装最新的 LTS 版本。

---

## 🚀 快速启动（首次运行）

### 步骤 1：打开两个终端窗口

你需要同时运行后端和前端，所以需要两个独立的终端窗口。

**Windows 用户：**
- 打开两个 PowerShell 或 CMD 窗口
- 或使用 VS Code 的集成终端，点击右上角 "+" 号新建终端

**Mac/Linux 用户：**
- 打开两个终端窗口
- 或使用 tmux/screen 等工具

---

### 步骤 2：启动后端服务

**在第一个终端窗口中：**

```bash
# 1. 进入项目根目录
cd D:\code\软件工程\MSE

# 2. 进入后端目录
cd backend

# 3. 安装依赖（首次运行需要，以后不需要）
npm install

# 4. 启动后端开发服务器
npm run dev
```

**成功标志：**
你会看到类似这样的输出：
```
Server is running on http://localhost:3000
Health check: http://localhost:3000/health
```

**⚠️ 保持这个终端窗口打开！不要关闭！**

---

### 步骤 3：启动前端服务

**在第二个终端窗口中：**

```bash
# 1. 进入项目根目录
cd D:\code\软件工程\MSE

# 2. 进入前端目录
cd frontend

# 3. 安装依赖（首次运行需要，以后不需要）
npm install

# 4. 启动前端开发服务器
npm run dev
```

**成功标志：**
你会看到类似这样的输出：
```
VITE v7.2.2  ready in 307 ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

**⚠️ 保持这个终端窗口也打开！**

---

### 步骤 4：访问应用

打开浏览器，访问：
```
http://localhost:5173
```

你应该能看到低代码平台的界面！

---

## 🔄 日常启动（第二次及以后）

以后每次启动项目，只需要执行以下步骤：

### 终端 1 - 启动后端
```bash
cd D:\code\软件工程\MSE\backend
npm run dev
```

### 终端 2 - 启动前端
```bash
cd D:\code\软件工程\MSE\frontend
npm run dev
```

**注意：** 不需要再运行 `npm install`，除非 `package.json` 有更新。

---

## 🛑 停止服务

### 方法 1：使用快捷键（推荐）
在终端窗口中按：
- **Windows**: `Ctrl + C`
- **Mac/Linux**: `Ctrl + C`

### 方法 2：关闭终端窗口
直接关闭终端窗口也会停止服务。

---

## ✅ 验证启动成功

### 1. 检查后端是否正常

打开浏览器或新终端，访问：
```bash
# 方法 1：浏览器访问
http://localhost:3000/health

# 方法 2：命令行测试
curl http://localhost:3000/health
```

**预期输出：**
```json
{"status":"ok","timestamp":"2025-11-19T14:30:00.000Z"}
```

### 2. 检查前端是否正常

浏览器访问：
```
http://localhost:5173
```

你应该能看到一个带导航栏的页面。

### 3. 检查数据库是否有数据

```bash
curl http://localhost:3000/api/schemas
```

**预期输出：**
应该能看到 `work-order` Schema 的 JSON 数据。

---

## 📂 项目目录结构

```
D:\code\软件工程\MSE\
│
├── backend/              # 后端项目
│   ├── prisma/          # 数据库相关
│   │   ├── schema.prisma    # 数据模型
│   │   ├── migrations/      # 迁移文件
│   │   └── dev.db          # SQLite 数据库文件
│   ├── src/             # 源代码
│   ├── package.json     # 后端依赖
│   └── .env            # 环境变量
│
├── frontend/            # 前端项目
│   ├── src/            # 源代码
│   ├── package.json    # 前端依赖
│   └── .env           # 环境变量
│
├── 开发文档.md          # 完整开发文档
├── 功能特性清单.md      # 功能列表
├── 项目完成总结.md      # 项目总结
└── 测试指南.md          # 测试指南
```

---

## 🔧 常见问题排查

### 问题 1：端口被占用

**错误信息：**
```
Error: listen EADDRINUSE: address already in use :::3000
```
或
```
Port 5173 is in use, trying another one...
```

**解决方案：**

**Windows:**
```bash
# 查找占用端口的进程
netstat -ano | findstr :3000
# 或
netstat -ano | findstr :5173

# 杀死进程（PID 是上一步显示的进程号）
taskkill /PID <进程号> /F
```

**Mac/Linux:**
```bash
# 查找并杀死占用端口的进程
lsof -ti:3000 | xargs kill -9
lsof -ti:5173 | xargs kill -9
```

**或者使用 npx：**
```bash
npx kill-port 3000 5173
```

---

### 问题 2：npm install 失败

**可能原因：**
- 网络问题
- npm 镜像源速度慢

**解决方案：**

切换到国内镜像源（推荐淘宝镜像）：
```bash
# 临时使用
npm install --registry=https://registry.npmmirror.com

# 永久设置
npm config set registry https://registry.npmmirror.com
```

---

### 问题 3：数据库文件丢失或损坏

**解决方案：**

重新初始化数据库：
```bash
cd backend

# 1. 删除旧数据库（如果存在）
rm prisma/dev.db

# 2. 运行迁移创建新数据库
npx prisma migrate dev --name init

# 3. 初始化示例数据
npm run seed

# 4. 创建基础数据（用户、角色、设备）
npx tsx src/createBaseData.ts
```

---

### 问题 4：前端页面空白或报错

**解决方案：**

1. **清理缓存：**
```bash
cd frontend
rm -rf node_modules/.vite
npm run dev
```

2. **重新安装依赖：**
```bash
cd frontend
rm -rf node_modules
npm install
npm run dev
```

3. **检查浏览器控制台：**
- 按 F12 打开开发者工具
- 查看 Console 标签中的错误信息
- 查看 Network 标签确认 API 请求是否成功

---

### 问题 5：API 请求失败（CORS 错误）

**错误信息：**
```
Access to fetch at 'http://localhost:3000/api/schemas' from origin 'http://localhost:5173' has been blocked by CORS policy
```

**解决方案：**

这种情况不应该发生（后端已配置 CORS），但如果出现：

1. 检查后端是否正在运行
2. 检查前端 `.env` 文件：
```bash
# frontend/.env
VITE_API_URL=http://localhost:3000
```
3. 重启后端和前端服务

---

### 问题 6：TypeScript 编译错误

**解决方案：**

```bash
# 后端
cd backend
npm run build  # 检查是否有编译错误

# 前端
cd frontend
npx tsc --noEmit  # 只检查类型，不编译
```

---

## 📝 开发技巧

### 1. 使用 VS Code

推荐使用 Visual Studio Code 开发：

1. 打开项目文件夹：`D:\code\软件工程\MSE`
2. 安装推荐插件：
   - ESLint
   - Prettier
   - Prisma
3. 使用内置终端（`` Ctrl + ` ``）
4. 可以同时打开多个终端窗口

### 2. 查看实时日志

两个终端窗口会实时显示：
- **后端终端**：API 请求日志、数据库操作
- **前端终端**：Vite 热更新、编译信息

### 3. 自动重启

- **后端**：使用 nodemon，修改代码后自动重启
- **前端**：使用 Vite HMR，修改代码后浏览器自动刷新

### 4. 数据库管理

查看和编辑数据库：
```bash
cd backend
npx prisma studio
```
会在浏览器打开一个可视化数据库管理界面。

---

## 🎯 快速命令参考

### 后端常用命令
```bash
npm run dev              # 启动开发服务器
npm run build            # 编译 TypeScript
npm run start            # 运行编译后的代码
npm run seed             # 初始化示例 Schema
npx tsx src/createBaseData.ts  # 创建基础数据
npx prisma studio        # 打开数据库管理界面
npx prisma migrate dev   # 运行数据库迁移
```

### 前端常用命令
```bash
npm run dev              # 启动开发服务器
npm run build            # 构建生产版本
npm run preview          # 预览生产版本
```

---

## 🌐 访问地址

- **前端应用**: http://localhost:5173
- **后端 API**: http://localhost:3000
- **健康检查**: http://localhost:3000/health
- **Schema API**: http://localhost:3000/api/schemas
- **数据 API**: http://localhost:3000/api/data/:entity
- **Prisma Studio**: http://localhost:5555 (运行 `npx prisma studio` 后)

---

## 📚 相关文档

- `开发文档.md` - 完整的技术文档和API说明
- `功能特性清单.md` - 所有功能的详细列表
- `测试指南.md` - 如何测试系统
- `项目完成总结.md` - 项目概览和总结

---

## ❓ 需要帮助？

如果遇到问题：

1. 查看终端输出的错误信息
2. 检查浏览器控制台（F12）
3. 参考上面的"常见问题排查"
4. 查看相关文档

---

**祝你启动顺利！** 🚀
